# Haven Clinic — landing page

Site único da Haven Clinic. O kit em `AI-Design-Studio-Starter-Kit/` é só referência de marca e briefing.

```powershell
Set-Location -LiteralPath '.\website'
npm install
npm run dev
```

Abra `http://localhost:5173`.
